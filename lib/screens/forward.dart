import 'package:flutter/material.dart';

class ForwardTab extends StatelessWidget {
  const ForwardTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
